// The TypeScript compiler will automatically infer the type of the variables based on the values assigned to them

var firstName = "This is First name";
var age = 23;

// even we did'nt decleare the type of the veriables it take automatically infer
// it show error

// age = "ten";
// firstName = 23;

console.log(firstName,age)