var text = "hi this is string can't  be Change";
var count = 23;
var isTrue = true;
// we can't change a types of veriables (it show error in typescript)
// text = isTrue;
// count = text;
// isTrue=number;
console.log(text, count, isTrue);
