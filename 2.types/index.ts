var text:string = "hi this is string can't  be Change";
var count:number = 23;
var isTrue:boolean = true;

// we can't change a types of veriables (it show error in typescript)

// text = isTrue;
// count = text;
// isTrue=number;

console.log(text,count,isTrue);